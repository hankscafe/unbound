# Screenshots

Referenced by the main `README.md`.

| File | Page |
| --- | --- |
| `login.png` | Login (book-cover collage background) |
| `dashboard.png` | Dashboard (stat cards, storage health, activity feed) |
| `library.png` | Library (badges, filters, pagination) |
| `accounts.png` | Accounts |
| `settings.png` | Settings (tabbed) |

These are captured against a **throwaway demo instance seeded with public-domain sample data**
(not real library data) so they're safe for a public repo. To regenerate after UI changes, run a
seeded demo and screenshot it headlessly (Playwright), then optimize to 1600px wide.
